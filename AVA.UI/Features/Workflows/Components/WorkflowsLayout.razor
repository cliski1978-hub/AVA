<div class="workflows-placeholder">
    <div class="workflows-placeholder__icon">W</div>
    <div class="workflows-placeholder__title">Workflows</div>
    <div class="workflows-placeholder__hint">Workflow management will be available in a future update.</div>
</div>

<style>
    .workflows-placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
        gap: var(--RxMd);
        color: var(--TextMuted);
    }

    .workflows-placeholder__icon {
        font-size: 48px;
        font-weight: 700;
        color: #22c55e;
        opacity: 0.5;
    }

    .workflows-placeholder__title {
        font-size: var(--FzLg);
        font-weight: 600;
        color: var(--Text);
    }

    .workflows-placeholder__hint {
        font-size: var(--FzSm);
        text-align: center;
        max-width: 360px;
    }
</style>
